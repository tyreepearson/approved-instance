# approved-instance
Terraform repo to build a examples to demo within Terraform Cloud. 

# What this includes
    - providers:
      - AWS 
      - HCP
    - resources:
      - instances (aws) 
      - modules (ecs)
    - data sources: 
      - hcp packer (image)
